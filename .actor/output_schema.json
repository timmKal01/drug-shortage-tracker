{
    "actorOutputSchemaVersion": 1,
    "title": "Output schema of Drug Shortage Tracker",
    "description": "One record per drug shortage entry from the FDA's openFDA shortage database.",
    "properties": {
        "results": {
            "type": "string",
            "title": "Results",
            "template": "{{links.apiDefaultDatasetUrl}}/items"
        }
    }
}
